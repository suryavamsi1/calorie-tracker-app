# Base URL for the BiteLog API server. See src/lib/api.ts for platform notes
# (Android emulator needs 10.0.2.2, physical devices need your machine's LAN IP).
EXPO_PUBLIC_API_URL=http://localhost:4000
